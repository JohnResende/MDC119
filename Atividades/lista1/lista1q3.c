#include <stdlib.h>
#include <conio.h>
#include <stdio.h>

int main()
{

    int anterior;
    int numero = 0;
    int proximo = 1;
    int i;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    printf("a variavel numero e: %d", numero);
    anterior = proximo;
    proximo = numero + proximo;
    numero = anterior;

    return 0;
}